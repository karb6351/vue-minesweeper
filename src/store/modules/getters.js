const getBorderSize = state => state.borderSize

const getIsFirstClick = state => state.isFirstClick

const getBombQuantity = state => state.bombQuantity

export default {
    getBorderSize,
    getIsFirstClick,
    getBombQuantity
}